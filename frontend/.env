# Leave empty for local Vite dev (API requests are proxied to the backend).
# Docker/production builds pass VITE_API_URL=http://localhost:8000 at build time.
VITE_API_URL=
MAILCATCHER_HOST=http://localhost:1080
